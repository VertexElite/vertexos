# VertexOS package list
# Format: one package per line, # for comments.
# Consumed by: build/mklive-wrapper.sh -> mklive.sh -p "$(cat packages.txt)"

# === Live-system + installer essentials ===
# These are what makes the live ISO usable BEFORE install completes:
# DHCP client, ping, partitioning, GUI install loader (Calamares: Phase 2),
# console-mode TUI installer fallback (void-installer). Every other distro
# (Ubuntu, Arch, Manjaro) ships this minimum baseline pre-installed on the
# live image — we were missing it.
void-installer
dhcpcd
iproute2
iputils
parted
gptfdisk
cryptsetup
kbd
nano
less
man-db
pciutils
usbutils
acpid
polkit
elogind
wpa_supplicant
iw

# === Security core ===
# (linux-hardened srcpkg planned for Phase 1; stock linux 6.18 already ships
#  every KSPP hardening flag Chimera's linux-lts uses — see docs/kernel-config.md)
linux
linux-firmware-amd
linux-firmware-intel
apparmor
audit
nftables
stubby
usbguard

# Detection / monitoring (Phase 2-3)
# falco          # not yet packaged in Void; src build planned
# suricata
# zeek
# ntopng
clamav
aide
rkhunter
chkrootkit
firejail

# === Desktop ===
xfce4
xfce4-plugins
xfce4-pulseaudio-plugin
thunar-volman
xorg-minimal
xorg-input-drivers
xorg-video-drivers
sddm
picom
network-manager-applet
NetworkManager
dbus
dbus-x11
mesa-dri
xauth
xterm

# Fonts + theme deps so XFCE renders text properly out of the box
noto-fonts-ttf
noto-fonts-emoji
dejavu-fonts-ttf
liberation-fonts-ttf
font-bitstream-vera
gtk-engine-murrine
gnome-themes-extra

# GUI remote access (so installed-to-disk system is reachable from VBox / metal)
tigervnc

# === Network tools ===
iftop
nethogs
bmon
tcpdump
wireshark
# nmap is not packaged in Void; the following cover its host-discovery and
# port-scanning roles:
#   masscan    - fast asynchronous TCP port scanner
#   arp-scan   - layer-2 LAN host discovery
#   netscanner - modern TUI: discovery, port scan, packet capture
masscan
arp-scan
netscanner
mtr

# === Base tools ===
git
curl
wget
vim
htop
tmux
ripgrep
fd
jq
xtools
bash-completion
sudo

# === Filesystem ===
e2fsprogs
btrfs-progs
dosfstools
exfatprogs
lvm2

# === Boot / firmware ===
grub-x86_64-efi
grub-i386-efi
efibootmgr
sbctl
