# VertexOS AppArmor parser config — enforcement defaults.
# Forces every profile loaded by apparmor_parser to ENFORCE mode unless the
# profile itself explicitly declares 'complain' or 'audit' flags.

# Default action on profile load
enforce_default = enforce

# Reject any profile with no explicit mode (would otherwise default to complain)
# warn = rules
# warn = include
# warn = override
