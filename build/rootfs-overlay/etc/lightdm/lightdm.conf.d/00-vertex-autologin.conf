# VertexOS autologin override.
# Drop-in under /etc/lightdm/lightdm.conf.d/ — read AFTER /etc/lightdm/lightdm.conf
# so this beats any mkiso-generated defaults that overwrite the main file.

[Seat:*]
autologin-user=vertex
autologin-user-timeout=0
autologin-session=xfce
autologin-guest=false
