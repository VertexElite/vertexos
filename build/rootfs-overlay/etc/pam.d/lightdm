#%PAM-1.0
# VertexOS lightdm greeter PAM (glibc). system-login pulls pam_elogind so the
# greeter session gets a logind seat - fixes the musl-era greeter login loop.
auth       include    system-login
account    include    system-login
password   include    system-login
session    include    system-login
