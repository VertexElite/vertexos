# security/falco/

Falco rules for the VertexSense runtime-threat sensor.

`falco` is not yet packaged in Void; a source build is planned (see
`build/packages.txt`). Custom `*_rules.yaml` drop-ins live here and load from
`/etc/falco/rules.d/`.

Empty for now — Phase 2-3.
