# security/suricata/

Suricata IDS/IPS rules and config for VertexOS.

Custom rules + `suricata.yaml` tuning live here and install to
`/etc/suricata/`. Feeds the VertexSense dashboard.

Empty for now — Phase 2-3 (Suricata not yet in `build/packages.txt`).
